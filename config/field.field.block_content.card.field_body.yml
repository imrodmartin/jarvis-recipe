langcode: en
status: true
dependencies:
  config:
    - block_content.type.card
    - field.storage.block_content.field_body
  module:
    - text
id: block_content.card.field_body
field_name: field_body
entity_type: block_content
bundle: card
label: Body
description: ''
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  # Restricted to the theme's own formats. An empty list means every format the
  # editor has access to, which included Full HTML and the locked Canvas formats.
  allowed_formats:
    - jarvis_html
    - jarvis_full_html
field_type: text_long
